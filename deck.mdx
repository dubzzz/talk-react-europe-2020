import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer";
import { github, vsDark } from "@code-surfer/themes";
import { Split } from 'mdx-deck'

import NavigationHell from './src/NavigationHell'
import WelcomeSlide from './src/WelcomeSlide'
import WhoAmI from './src/WhoAmI'

import BoomVideo from './images/boom.webm'
import AndroidBug from './images/lockscreen-guardian.png'
import FaceTimeBug from './images/facetime-nyt.png'
import CoverageImage from './images/coverage.jpg'

export const theme = vsDark;

<WelcomeSlide />

---

### One day, it happened...

---

<video controls width="100%" style={{ cursor: 'default' }}>
  <source src={BoomVideo} type="video/webm" />
</video>

---

<Split>

<img src={AndroidBug} alt="Android bug" />

<p style={{textAlign:'left'}}>
  0- Lock your screen
  <br/>1- Open the EMERGENCY CALL window
  <br/>2- Type a few characters
  <br/>3- Select them and copy-paste <i>until no longer highlights</i>
  <br/>4- Go back to the lockscreen
  <br/>5- Open the camera
  <br/>6- Open the notification drawer
  <br/>7- Tap the Settings (gear) icon
  <br/>8- Long-tap in the password field and paste the characters into it <i>until it crashes</i> 💥
</p>

</Split>

---

<Split>

<p style={{textAlign:'left'}}>
  0- Start a FaceTime video call
  <br/>1- Swipe up
  <br/>2- Tap “Add Person” <i>before the call is answered</i>
  <br/>3- Add yourself
  <br/>
  <br/><i>You can now listen the user on the other end</i> 💥
</p>

<img src={FaceTimeBug} alt="FaceTime bug" />

</Split>

---

<CodeSurfer>

```js file=./snippets/isSubstring.js title="Unit Test"
```

```js 5[22:27],6[22:31],7[22:35],8[22:35],9[22:35],10[22:36],11[22:36] file=./snippets/isSubstring.js title="Unit Test" subtitle="Inputs"
```

```js 5[36:39],6[40:43],7[44:47],8[44:47],9[44:47],10[45:49],11[45:49] file=./snippets/isSubstring.js title="Unit Test" subtitle="Outputs"
```

</CodeSurfer>

---

<CodeSurfer>

```js file=./snippets/cypress.js title="End-To-End Test"
```

```js 3,5[25:31],10 file=./snippets/cypress.js title="End-To-End Test" subtitle="Actions"
```

```js 3,5[25:31],10 file=./snippets/cypress.js title="End-To-End Test" subtitle="'Inputs'"
```

```js 5[5:23],7,9,11 file=./snippets/cypress.js title="End-To-End Test" subtitle="Expectations"
```

```js 5[5:23],7,9,11 file=./snippets/cypress.js title="End-To-End Test" subtitle="'Outputs'"
```

</CodeSurfer>

---

### Are we sure to cover everything?

Yes, all my users are happy

Yes, I have 100% coverage

---

<NavigationHell />

My users are happy

---

<img src={CoverageImage} alt="Coverage 100%" />

100% coverage

---

### Not enough...

---

<WhoAmI />

---

# Hello 👋

You are using [MDX Deck](https://github.com/jxnblk/mdx-deck) and [Code Surfer](https://codesurfer.pomb.us)

---

<CodeSurfer>

```js title="This is a title" subtitle="and this a subtitle"
function lorem(ipsum, dolor = 1) {
  const sit = ipsum == null ? 0 : ipsum.sit;
  dolor = sit - amet(dolor);
  return sit
    ? consectetur(ipsum, 0, dolor < 0 ? 0 : dolor)
    : [];
}

function incididunt(ipsum, ut = 1) {
  ut = labore.et(amet(ut), 0);
  const sit = ipsum == null ? 0 : ipsum.sit;

  if (!sit || ut < 1) {
    return [];
  }

  let dolore = 0;
  let magna = 0;
  const aliqua = new eiusmod(labore.ut(sit / ut));

  while (dolore < sit) {
    aliqua[magna++] = consectetur(
      ipsum,
      dolore,
      (dolore += ut)
    );
  }

  return aliqua;
}
```

```js
function lorem(ipsum, dolor = 1) {
  const sit = ipsum == null ? 0 : ipsum.sit;
  dolor = sit - amet(dolor);
  return sit
    ? consectetur(ipsum, 0, dolor < 0 ? 0 : dolor)
    : [];
}

function adipiscing(...elit) {
  if (!elit.sit) {
    return [];
  }

  const sed = elit[0];
  return eiusmod.tempor(sed) ? sed : [sed];
}

function incididunt(ipsum, ut = 1) {
  ut = labore.et(amet(ut), 0);
  const sit = ipsum == null ? 0 : ipsum.sit;

  if (!sit || ut < 1) {
    return [];
  }

  let dolore = 0;
  let magna = 0;
  const aliqua = new eiusmod(labore.ut(sit / ut));

  while (dolore < sit) {
    aliqua[magna++] = consectetur(
      ipsum,
      dolore,
      (dolore += ut)
    );
  }

  return aliqua;
}
```

```diff 1[10:14],2[15:19],3[22:27],10:12

```

</CodeSurfer>

---

<CodeSurferColumns themes={[vsDark, github]}>

<Step>

```js
const magna = aliqua => aliqua.ut((enim, ad) => enim, 0);
```

```js
const minim = (ad, enim) => dolore.magna(ad / enim);
```

</Step>

<Step>

```js
const lorem = (ipsum, dolor, sit) => {
  const amet = dolor - ipsum;
  return consectetur.adipiscing(
    {
      elit: sed.eiusmod(sit - dolor) / amet + 2,
    },
    (tempor, incididunt) => ipsum + amet * incididunt
  );
};

const magna = aliqua => aliqua.ut((enim, ad) => enim, 0);
```

```js
const minim = (ad, enim) => dolore.magna(ad / enim);

const sed = (eiusmod, tempor, incididunt) => {
  const ut = tempor - eiusmod;
  return labore.et(
    {
      amet: dolore.magna(incididunt - tempor) / ut + 2,
    },
    (aliqua, elit) => eiusmod + ut * elit
  );
};
```

</Step>

</CodeSurferColumns>

---

docs:  
[codesurfer.pomb.us](https://codesurfer.pomb.us)
